.scroll-down-btn {
  background: transparent;
  color: $text-color;
  &:after {
    content: "";
    display: inline-flex;
    background: svg-load('scroll.svg', fill=$text-color, width=100%, height=100%);
    width: 13px;
    height: 13px;
    margin-left: 15px;
  }
}